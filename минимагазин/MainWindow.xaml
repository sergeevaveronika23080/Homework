<Window x:Class="минимагазин.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:минимагазин"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel Margin="10">
        <Label DockPanel.Dock="Top"
               Content="Управление товарами"
               FontSize="18" HorizontalAlignment="Center"
               Margin="0,0,0,10"/>

        <StackPanel DockPanel.Dock="Left" 
                    Width="120" 
                    Margin="5">
            <Button Content="Добавить товар" 
                    Click="Add_Click"
                    Margin="0,0,0,5"/>
            <Button Content="Удалить" 
                    Click="Del_Click"
                    Margin="0,0,0,5"/>
            <Button Content="Выход" 
                    Click="Exit_Click"/>
        </StackPanel>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" Content="Название:"/>
                <TextBox Grid.Column="1" x:Name="NameInput" Width="120"/>

                <Label Grid.Column="2" Content="Цена:"/>
                <TextBox Grid.Column="3" x:Name="PriceInput" Width="80"/>
            </Grid>

            <DataGrid Grid.Row="1" 
                      x:Name="ProductsGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Товар" 
                                        Width="*" 
                                        Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Цена" 
                                        Width="*" 
                                        Binding="{Binding Price}" />
                    <DataGridCheckBoxColumn Header="В наличии" 
                                            Width="*" 
                                            Binding="{Binding InStock}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</Window>   